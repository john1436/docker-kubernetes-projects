apiVersion: v1

kind: Service

metadata:
  name: backend-service
  namespace: task-app

spec:
  type: LoadBalancer

  selector:
    app: backend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
